NOTE:
Check the whitespaces in the CSV file. It looked like the column headers have a space before and after.
Should work by stripping any non numbers from the CSV file name. Just make sure each file has the voltage number somewhere inside AND that the voltages are the ONLY numbers.